platform :ios, '9.0'

target 'FireEats' do

  use_frameworks!

  pod 'SDWebImage'
  pod 'FirebaseUI/Auth'
  pod 'FirebaseUI/Google'
  pod 'Firestore', :podspec => 'https://storage.googleapis.com/firebase-preview-drop/ios/firestore/0.6.1/Firestore.podspec.json'
  
  target 'FireEatsTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
